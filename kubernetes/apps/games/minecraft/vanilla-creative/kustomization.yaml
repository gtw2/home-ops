---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
  - ./route-service.yaml
# Mod configs are inherited by referencing minecraft-vanilla-configmap from the vanilla kustomization;
# no configMapGenerator here to keep mod settings in lockstep with the source server.
