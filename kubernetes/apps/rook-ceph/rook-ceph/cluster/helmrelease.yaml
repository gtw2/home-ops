---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rook-ceph-cluster
spec:
  chartRef:
    kind: OCIRepository
    name: rook-ceph-cluster
  interval: 1h
  values:
    monitoring:
      enabled: true
      createPrometheusRules: true
    toolbox:
      enabled: true
    route:
      dashboard:
        host:
          name: rook.${SECRET_DOMAIN}
          path: /
          pathType: PathPrefix
        parentRefs:
          - name: envoy-internal
            namespace: network
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Storage
          gethomepage.dev/name: Ceph
          gethomepage.dev/icon: ceph.png
          gethomepage.dev/description: Ceph storage dashboard
          gethomepage.dev/pod-selector: app=rook-ceph-mgr
    cephClusterSpec:
      cephConfig:
        global:
          bdev_enable_discard: "true" # quote
          bdev_async_discard_threads: "1" # quote
          osd_class_update_on_start: "false" # quote
          device_failure_prediction_mode: local # requires mgr module
      cleanupPolicy:
        wipeDevicesFromOtherClusters: true
      crashCollector:
        disable: false
      csi:
        readAffinity:
          enabled: true
      security:
        cephx:
          daemon:
            # Rotate daemon (mon/mgr/osd/mds) keys to the secure aes256k
            # cipher introduced in Ceph 20.2 Tentacle. Clears the HEALTH_ERR
            # AUTH_INSECURE_SERVICE_KEY_TYPE.
            keyRotationPolicy: KeyGeneration
            keyGeneration: 2
          csi:
            # Rotate the four CSI client keys (rbd/cephfs, node/provisioner)
            # to the secure aes256k cipher for CVE-2025-30156.
            #
            # keyType MUST be set explicitly. The rook-ceph-cluster chart
            # defaults it to `aes` ("required when Kubernetes nodes don't run
            # Linux kernel 7.0+"), and that default overrides the mon map's
            # auth_preferred_cipher. Omitting it here does not fall back to
            # the preferred cipher — it silently re-applies aes, which is how
            # generation 2 ended up insecure. Talos 1.14 backported aes256k
            # to its 6.18 kernel, so the chart default's premise is not true
            # for this cluster.
            keyType: aes256k
            # Generation bump makes the rotation deterministic; the CRD only
            # says changing keyType "may" initiate one.
            keyRotationPolicy: KeyGeneration
            keyGeneration: 3
            # Keeps generation 2 valid during migration. Prior keys are NOT
            # retired automatically at 1 — a follow-up must set this to 0 to
            # delete the leftover aes keys, and only then will
            # AUTH_INSECURE_CLIENT_KEY_TYPE clear. Do that once RBD and
            # CephFS mounts are confirmed working on the aes256k key.
            keepPriorKeyCountMax: 1
          rbdMirrorPeer:
            # Done: rotated to generation 2 / aes256k and no longer reported
            # by AUTH_INSECURE_CLIENT_KEY_TYPE. It has no chart default for
            # keyType, so it correctly picked up the mon map's
            # auth_preferred_cipher — which is what csi above could not.
            # No keepPriorKeyCountMax here; the CRD only offers it on csi.
            keyRotationPolicy: KeyGeneration
            keyGeneration: 2
          # NOTE: allowedCiphers is deliberately NOT set here. Restricting it
          # to [aes256k] removes aes from the mon map and locks out any client
          # still holding an aes key — CSI included. It is a separate change,
          # made only once `ceph health detail` reports zero entities on aes.
          # Recovery from a lockout is mon_auth_emergency_allowed_ciphers,
          # which is not runtime-settable and needs a mon restart.
      healthCheck:
        muteHealthWarning:
          # Talos 1.14 backported aes256k to its 6.18 kernel
          # (siderolabs/pkgs@84c1b87), so the kernel-client gate is cleared —
          # crypto_krb5_* and ceph_crypto_key_prepare are present on all four
          # nodes. These mutes stay until the CSI rotation above completes and
          # allowedCiphers is restricted; unmute one at a time as each clears.
          AUTH_INSECURE_CLIENT_KEY_TYPE:
            policy: mute
          AUTH_INSECURE_KEYS_ALLOWED:
            policy: mute
          AUTH_INSECURE_KEYS_CREATABLE:
            policy: mute
          AUTH_INSECURE_ROTATING_SERVICE_KEY_TYPE:
            policy: mute
      dashboard:
        enabled: true
        urlPrefix: /
        ssl: false
        prometheusEndpoint: http://prometheus-operated.observability.svc.cluster.local:9090
      mgr:
        modules:
          - name: pg_autoscaler
            enabled: true
          - name: rook
            enabled: true
      network:
        provider: host
        addressRanges:
          public:
            - 10.10.40.0/24
          cluster:
            - 10.10.40.0/24
        connections:
          requireMsgr2: true
      resources:
        mgr:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            memory: 2Gi
        osd:
          limits:
            memory: 8Gi
      storage:
        useAllNodes: true
        useAllDevices: false
        devicePathFilter: /dev/disk/by-id/nvme-SAMSUNG_MZQL21T9HCJR-.*
        config:
          osdsPerDevice: "1"
    cephBlockPools:
      - name: ceph-blockpool
        spec:
          failureDomain: host
          replicated:
            size: 3
        storageClass:
          enabled: true
          name: ceph-block
          isDefault: true
          reclaimPolicy: Delete
          allowVolumeExpansion: true
          volumeBindingMode: Immediate
          mountOptions:
            - discard
          parameters:
            compression_mode: aggressive
            compression_algorithm: zstd
            imageFormat: "2"
            imageFeatures: layering,fast-diff,object-map,deep-flatten,exclusive-lock
            csi.storage.k8s.io/provisioner-secret-name: rook-csi-rbd-provisioner
            csi.storage.k8s.io/provisioner-secret-namespace: "{{ .Release.Namespace }}"
            csi.storage.k8s.io/controller-expand-secret-name: rook-csi-rbd-provisioner
            csi.storage.k8s.io/controller-expand-secret-namespace: "{{ .Release.Namespace }}"
            csi.storage.k8s.io/node-stage-secret-name: rook-csi-rbd-node
            csi.storage.k8s.io/node-stage-secret-namespace: "{{ .Release.Namespace }}"
            csi.storage.k8s.io/fstype: ext4
    cephBlockPoolsVolumeSnapshotClass:
      enabled: true
      name: csi-ceph-blockpool
      isDefault: false
      deletionPolicy: Delete
    cephFileSystems:
      - name: &cephFileSystemName ceph-filesystem
        spec:
          metadataPool:
            replicated:
              size: 3
          dataPools:
            - failureDomain: host
              replicated:
                size: 3
              name: data0
          metadataServer:
            activeCount: 1
            activeStandby: true
            priorityClassName: system-cluster-critical
            placement:
              topologySpreadConstraints:
                - maxSkew: 1
                  topologyKey: kubernetes.io/hostname
                  whenUnsatisfiable: DoNotSchedule
                  labelSelector:
                    matchLabels:
                      app.kubernetes.io/name: ceph-mds
                      app.kubernetes.io/part-of: *cephFileSystemName
            resources:
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                memory: 4Gi
        storageClass:
          enabled: true
          isDefault: false
          name: ceph-filesystem
          pool: data0
          reclaimPolicy: Delete
          allowVolumeExpansion: true
          volumeBindingMode: Immediate
          parameters:
            csi.storage.k8s.io/provisioner-secret-name: rook-csi-cephfs-provisioner
            csi.storage.k8s.io/provisioner-secret-namespace: "{{ .Release.Namespace }}"
            csi.storage.k8s.io/controller-expand-secret-name: rook-csi-cephfs-provisioner
            csi.storage.k8s.io/controller-expand-secret-namespace: "{{ .Release.Namespace }}"
            csi.storage.k8s.io/node-stage-secret-name: rook-csi-cephfs-node
            csi.storage.k8s.io/node-stage-secret-namespace: "{{ .Release.Namespace }}"
            csi.storage.k8s.io/fstype: ext4
    cephFileSystemVolumeSnapshotClass:
      enabled: true
      name: csi-ceph-filesystem
      isDefault: false
      deletionPolicy: Delete
    cephObjectStores: []
