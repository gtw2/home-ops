---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
components:
  - ../../components/common
resources:
  # Retired in favour of ./warden (volsync has the data if it's ever needed).
  # - ./discordgsm/ks.yaml
  - ./echo/ks.yaml
  - ./homepage/ks.yaml
  - ./warden/ks.yaml
  # - ./picoshare/ks.yaml
